@model IEnumerable<Firma.Data.Data.CMS.PrzydatneLinki>

@{
    ViewData["Title"] = "Przydatne linki";
}

<div class="mt-4" style="max-width: 1500px; margin: 0 auto;">
    <h2 class="fw-bold mb-2">Przydatne linki</h2>
    <div class="mb-4">
        <a asp-action="Create" class="btn btn-success">
            <i class="bi bi-plus-lg me-1"></i> Dodaj nowy link
        </a>
    </div>

    <div class="card shadow-sm border-0 mb-4">
        <div class="card-body">
            <form class="row g-2 align-items-end">
                <div class="col-md-7">
                    <label for="search" class="form-label mb-1">Szukaj po tytule lub adresie</label>
                    <input type="text" id="search" class="form-control" placeholder="Wpisz tytuł lub adres URL..." />
                </div>
                <div class="col-md-3">
                    <label for="sort" class="form-label mb-1">Sortuj według</label>
                    <select id="sort" class="form-select">
                        <option>Domyślnie</option>
                        <option>Tytuł A-Z</option>
                        <option>Tytuł Z-A</option>
                        <option>Adres A-Z</option>
                        <option>Adres Z-A</option>
                    </select>
                </div>
                <div class="col-md-2 d-flex align-items-end">
                    <button type="button" class="btn btn-primary w-100">Filtruj / Sortuj</button>
                </div>
            </form>
        </div>
    </div>

    <div class="table-responsive shadow-sm rounded-3">
        <table class="table table-hover align-middle mb-0">
            <thead class="table-light">
                <tr>
                    <th style="width: 40%;">Tytuł</th>
                    <th style="width: 40%;">Adres URL</th>
                    <th style="width: 20%;" class="text-end">Akcje</th>
                </tr>
            </thead>
            <tbody>
                @if (!Model.Any())
                {
                    <tr>
                        <td colspan="3" class="text-center text-muted py-4">
                            Brak linków do wyświetlenia.
                        </td>
                    </tr>
                }
                else
                {
                    foreach (var item in Model)
                    {
                        <tr>
                            <td>@item.Tytul</td>
                            <td>
                                <a href="@item.Link" target="_blank" rel="noopener" class="text-decoration-underline">
                                    @item.Link
                                </a>
                            </td>
                            <td class="text-end">
                                <a asp-action="Edit" asp-route-id="@item.IdPrzydatneLinki" class="btn btn-sm btn-outline-primary me-1" title="Edytuj">
                                    <i class="bi bi-pencil">Edytuj</i>
                                </a>
                                <a asp-action="Details" asp-route-id="@item.IdPrzydatneLinki" class="btn btn-sm btn-outline-info me-1" title="Szczegóły">
                                    <i class="bi bi-eye">Szczegóły</i>
                                </a>
                                <a asp-action="Delete" asp-route-id="@item.IdPrzydatneLinki" class="btn btn-sm btn-outline-danger" title="Usuń">
                                    <i class="bi bi-trash">Usuń</i>
                                </a>
                            </td>
                        </tr>
                    }
                }
            </tbody>
        </table>
    </div>
</div>
