@model Firma.Intranet.Models.Sklep.ProduktPromocja

@{
    ViewData["Title"] = "Usuń powiązanie produktu z promocją";
}

<div class=" mt-4">
    <div class="row justify-content-center">
        <div class="col-md-7 col-lg-5">
            <div class="card shadow-sm border-0">
                <div class="card-body">
                    <h2 class="fw-bold text-danger mb-3">Usuwanie powiązania</h2>
                    <div class="alert alert-warning d-flex align-items-center mb-4" role="alert">
                        <i class="bi bi-exclamation-triangle-fill me-2 fs-4"></i>
                        <div>
                            <strong>Czy na pewno chcesz usunąć to powiązanie?</strong><br />
                            Tej operacji nie można cofnąć!
                        </div>
                    </div>
                    <dl class="row mb-4">
                        <dt class="col-sm-4 text-muted">Kod towaru</dt>
                        <dd class="col-sm-8">@Model.Towar?.Kod</dd>
                        <dt class="col-sm-4 text-muted">Nazwa promocji</dt>
                        <dd class="col-sm-8">@Model.Promocja?.Nazwa</dd>
                    </dl>
                    <form asp-action="Delete">
                        <input type="hidden" asp-for="IdProduktyPromocji" />
                        <div class="d-flex justify-content-between">
                            <button type="submit" class="btn btn-danger w-100 me-2">
                                <i class="bi bi-trash me-1"></i> Usuń powiązanie
                            </button>
                            <a asp-action="Index" class="btn btn-primary w-100">Powrót</a>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>
