@model Firma.Intranet.Models.Sklep.ProduktPromocja

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ProduktPromocja</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="IdProduktyPromocji" />
            <div class="form-group">
                <label asp-for="IdTowaru" class="control-label"></label>
                <select asp-for="IdTowaru" class="form-control" asp-items="ViewBag.IdTowaru"></select>
                <span asp-validation-for="IdTowaru" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdPromocji" class="control-label"></label>
                <select asp-for="IdPromocji" class="form-control" asp-items="ViewBag.IdPromocji"></select>
                <span asp-validation-for="IdPromocji" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
